# Generador de Submittals ES — dependencias (v2.6.8)
# Una sola API: OpenAI (texto + Vision; seleccion automatica de modelo).
#
#   pip install -r requirements.txt
#   python -m playwright install chromium
#
openai
jinja2
playwright
pypdf
pymupdf
pytesseract
Pillow
python-docx
requests
beautifulsoup4
pdfplumber      # v2.6.5: relectura de tablas tecnicas densas (dimensiones/tolerancias)

# v3.0.0: la BD centralizada genera los Excel de guia -> openpyxl es REQUERIDO.
openpyxl

# --- Opcionales ---
# pyinstaller     # solo para compilar el .exe

# NOTA: el codigo NO usa anthropic, PyPDF2, pdf2image ni langdetect.
#  - SICOP usa OpenAI Vision (no Anthropic).
#  - La deteccion/traduccion de idioma la hace el propio modelo de OpenAI.
#  - OCR de imagenes/PDF escaneados requiere Tesseract-OCR (v2.6.6: bootstrap()
#    lo instala solo con "winget" si falta, y descarga el idioma espanol a la
#    carpeta propia "tessdata_es/" -- no requiere permisos de administrador).
packaging        # v2.6.7: comparar versiones (auto-updater)

# --- v3.1.0: BD sincronizada con GitHub -------------------------------------
# NO se agrega ninguna dependencia nueva. A proposito:
#   * git_bd.py usa el ejecutable "git" via subprocess (no GitPython). GitPython
#     tambien invoca el binario de git, asi que no aporta nada y complica
#     bastante el empaquetado con PyInstaller.
#   * El backend alternativo (PCs sin git) usa la API REST de GitHub con
#     urllib de la biblioteca estandar. Tampoco hace falta paramiko: la
#     autenticacion es HTTPS + Personal Access Token, no SSH.
#
# REQUISITO DEL SISTEMA (no de pip): para el modo preferido hay que tener
# instalado Git for Windows -> https://git-scm.com/download/win
# Si no esta, el app detecta la ausencia y sincroniza por la API REST.
